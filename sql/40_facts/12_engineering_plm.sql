-- Generated by tools.generator. Do not hand-edit.

-- Subject area: engineering_plm

IF OBJECT_ID(N'fact.BomComponentUsage', N'U') IS NULL
BEGIN
CREATE TABLE [fact].[BomComponentUsage] (
    [BomComponentUsageKey] BIGINT IDENTITY(1,1) NOT NULL,
    [EffectiveDateKey] INT NOT NULL DEFAULT -1,
    [ExpirationDateKey] INT NOT NULL DEFAULT -1,
    [BomHeaderKey] INT NOT NULL DEFAULT -1,
    [AssemblyProductKey] INT NOT NULL DEFAULT -1,
    [ComponentEngineeringPartRevisionKey] INT NOT NULL DEFAULT -1,
    [UnitOfMeasureKey] INT NOT NULL DEFAULT -1,
    [BomLineNumber] NVARCHAR(30) NOT NULL,
    [EventCount] INT NOT NULL,
    [QuantityAmount] DECIMAL(18,4) NULL,
    [DurationMinutes] DECIMAL(18,4) NULL,
    [CostAmount] MONEY NULL,
    [SourceSystemKey] INT NOT NULL DEFAULT -1,
    [BatchId] BIGINT NOT NULL DEFAULT 0,
    [LoadedAtUtc] DATETIME2(3) NOT NULL DEFAULT SYSUTCDATETIME(),
    CONSTRAINT [PK_fact_BomComponentUsage] PRIMARY KEY NONCLUSTERED ([BomComponentUsageKey])
);
END
GO

IF OBJECT_ID(N'fact.EngineeringChangeLifecycle', N'U') IS NULL
BEGIN
CREATE TABLE [fact].[EngineeringChangeLifecycle] (
    [EngineeringChangeLifecycleKey] BIGINT IDENTITY(1,1) NOT NULL,
    [CreatedDateKey] INT NOT NULL DEFAULT -1,
    [ApprovedDateKey] INT NOT NULL DEFAULT -1,
    [ReleasedDateKey] INT NOT NULL DEFAULT -1,
    [ClosedDateKey] INT NOT NULL DEFAULT -1,
    [EngineeringChangeOrderKey] INT NOT NULL DEFAULT -1,
    [OwnerEmployeeKey] INT NULL DEFAULT -1,
    [BusinessUnitKey] INT NOT NULL DEFAULT -1,
    [EngineeringChangeTypeKey] INT NOT NULL DEFAULT -1,
    [ApprovalStatusKey] INT NOT NULL DEFAULT -1,
    [EngineeringChangeOrderNumber] NVARCHAR(40) NOT NULL,
    [EventCount] INT NOT NULL,
    [QuantityAmount] DECIMAL(18,4) NULL,
    [DurationMinutes] DECIMAL(18,4) NULL,
    [CostAmount] MONEY NULL,
    [EngineeringChangeLifecycleLagDays] INT NULL,
    [SourceSystemKey] INT NOT NULL DEFAULT -1,
    [BatchId] BIGINT NOT NULL DEFAULT 0,
    [LoadedAtUtc] DATETIME2(3) NOT NULL DEFAULT SYSUTCDATETIME(),
    CONSTRAINT [PK_fact_EngineeringChangeLifecycle] PRIMARY KEY CLUSTERED ([EngineeringChangeLifecycleKey])
);
END
GO

IF OBJECT_ID(N'fact.EngineeringDocumentRevisionEvent', N'U') IS NULL
BEGIN
CREATE TABLE [fact].[EngineeringDocumentRevisionEvent] (
    [EngineeringDocumentRevisionEventKey] BIGINT IDENTITY(1,1) NOT NULL,
    [EventDateKey] INT NOT NULL DEFAULT -1,
    [EventTimeOfDayKey] INT NOT NULL DEFAULT -1,
    [EngineeringCadDocumentKey] INT NOT NULL DEFAULT -1,
    [EngineeringPartRevisionKey] INT NULL DEFAULT -1,
    [DesignerEmployeeKey] INT NULL DEFAULT -1,
    [EngineeringDocumentTypeKey] INT NOT NULL DEFAULT -1,
    [EngineeringLifecycleStatusKey] INT NOT NULL DEFAULT -1,
    [DocumentRevisionCode] NVARCHAR(30) NOT NULL,
    [EventCount] INT NOT NULL,
    [QuantityAmount] DECIMAL(18,4) NULL,
    [DurationMinutes] DECIMAL(18,4) NULL,
    [CostAmount] MONEY NULL,
    [SourceSystemKey] INT NOT NULL DEFAULT -1,
    [BatchId] BIGINT NOT NULL DEFAULT 0,
    [LoadedAtUtc] DATETIME2(3) NOT NULL DEFAULT SYSUTCDATETIME(),
    CONSTRAINT [PK_fact_EngineeringDocumentRevisionEvent] PRIMARY KEY NONCLUSTERED ([EngineeringDocumentRevisionEventKey])
);
END
GO

IF OBJECT_ID(N'fact.EngineeringSpecificationCoverage', N'U') IS NULL
BEGIN
CREATE TABLE [fact].[EngineeringSpecificationCoverage] (
    [EngineeringSpecificationCoverageKey] BIGINT IDENTITY(1,1) NOT NULL,
    [SnapshotDateKey] INT NOT NULL DEFAULT -1,
    [ProductKey] INT NOT NULL DEFAULT -1,
    [EngineeringDisciplineKey] INT NOT NULL DEFAULT -1,
    [EngineeringMaterialSpecificationKey] INT NOT NULL DEFAULT -1,
    [EngineeringLifecycleStatusKey] INT NOT NULL DEFAULT -1,
    [EventCount] INT NOT NULL,
    [QuantityAmount] DECIMAL(18,4) NULL,
    [DurationMinutes] DECIMAL(18,4) NULL,
    [CostAmount] MONEY NULL,
    [SourceSystemKey] INT NOT NULL DEFAULT -1,
    [BatchId] BIGINT NOT NULL DEFAULT 0,
    [LoadedAtUtc] DATETIME2(3) NOT NULL DEFAULT SYSUTCDATETIME(),
    CONSTRAINT [PK_fact_EngineeringSpecificationCoverage] PRIMARY KEY CLUSTERED ([EngineeringSpecificationCoverageKey])
);
END
GO

IF OBJECT_ID(N'fact.RoutingOperationStandard', N'U') IS NULL
BEGIN
CREATE TABLE [fact].[RoutingOperationStandard] (
    [RoutingOperationStandardKey] BIGINT IDENTITY(1,1) NOT NULL,
    [EffectiveDateKey] INT NOT NULL DEFAULT -1,
    [ExpirationDateKey] INT NOT NULL DEFAULT -1,
    [RoutingHeaderKey] INT NOT NULL DEFAULT -1,
    [RoutingOperationTypeKey] INT NOT NULL DEFAULT -1,
    [WorkCenterKey] INT NOT NULL DEFAULT -1,
    [UnitOfMeasureKey] INT NOT NULL DEFAULT -1,
    [RoutingOperationCode] NVARCHAR(40) NOT NULL,
    [EventCount] INT NOT NULL,
    [QuantityAmount] DECIMAL(18,4) NULL,
    [DurationMinutes] DECIMAL(18,4) NULL,
    [CostAmount] MONEY NULL,
    [SourceSystemKey] INT NOT NULL DEFAULT -1,
    [BatchId] BIGINT NOT NULL DEFAULT 0,
    [LoadedAtUtc] DATETIME2(3) NOT NULL DEFAULT SYSUTCDATETIME(),
    CONSTRAINT [PK_fact_RoutingOperationStandard] PRIMARY KEY CLUSTERED ([RoutingOperationStandardKey])
);
END
GO
