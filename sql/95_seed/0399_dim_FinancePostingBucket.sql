-- Generated by tools.seeder. Do not hand-edit.
-- Requires sqlcmd variable SeedRoot, e.g. -v SeedRoot="C:\path\to\seed" (or /path/to/seed).

IF OBJECT_ID(N'dim.FinancePostingBucket', N'U') IS NOT NULL
AND NOT EXISTS (SELECT 1 FROM [dim].[FinancePostingBucket])
BEGIN
    DECLARE @sql nvarchar(max) =
        N'BULK INSERT [dim].[FinancePostingBucket]
    FROM ''$(SeedRoot)/dim/FinancePostingBucket.csv''
    WITH (FORMAT = ''CSV'', FIRSTROW = 2, FIELDQUOTE = ''"'', ROWTERMINATOR = ''0x0a'', DATAFILETYPE = ''char'''
        + CASE WHEN SERVERPROPERTY('HostPlatform') = N'Windows'
               THEN N', CODEPAGE = ''65001''' ELSE N'' END
        + N', KEEPIDENTITY, TABLOCK);';
    BEGIN TRY
    SET IDENTITY_INSERT [dim].[FinancePostingBucket] ON;
        EXEC sys.sp_executesql @sql;
    SET IDENTITY_INSERT [dim].[FinancePostingBucket] OFF;
    END TRY
    BEGIN CATCH
    SET IDENTITY_INSERT [dim].[FinancePostingBucket] OFF;
        THROW;
    END CATCH
END
GO
